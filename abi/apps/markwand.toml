id = "markwand"
state = "stateful"
quiesce = "stop-units"
snapshot = "copy-filebrowser-db"
forward = "copy-retained-paths"
write_capture = "capture-post-cutover-writes"
reverse = "restore-retained-paths"
rpo = "0"
paths = [
  "~/.config/filebrowser/fb.db",
]
capabilities = []
